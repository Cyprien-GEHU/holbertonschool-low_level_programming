#include "main.h"

/**
 * print_last_digit - print the last digit of number integer
 * @ld : "ld" is the number of the integer
 *
 * Return: the last digit
 *
 */

int print_last_digit(int ld)
{
	int v;
	v = ld % 10;
	return (v);
}
