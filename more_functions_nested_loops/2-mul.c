#include "main.h"

/**
 * mul - multiplies two integers
 * @a : 'a' the first integer
 * @b : 'b' the second integer
 *
 * Return: the multiplies a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
